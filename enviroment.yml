name: simulador-elecciones-pba
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - streamlit>=1.28.0

  # === Data Processing ===
  - pandas>=2.0.0
  - numpy>=1.24.0

  # === Geospatial Data ===
  - geopandas>=0.13.0
  - shapely>=2.0.0
  - fiona>=1.9.0
  - pyproj>=3.5.0
  - rtree>=1.0.1

  # === Visualization ===
  - matplotlib>=3.7.0

  # === Configuration ===
  - configparser>=5.3.0

  # === Optional: Performance Enhancements ===
  # - openpyxl>=3.1.0       # Excel file support
  # - fastparquet>=0.8.0    # Parquet file support
  # - psutil>=5.9.0         # System resource monitoring

  # === pip-only packages ===
  - pip
  - pip:
      - poli-sci-kit>=1.1.0
